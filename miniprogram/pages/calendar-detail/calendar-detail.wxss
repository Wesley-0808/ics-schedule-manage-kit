/* miniprogram/pages/calendar-detail/calendar-detail.wxss */
.container {
  padding: 20px;
}

.calendar-color-indicator {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  display: inline-block;
  vertical-align: middle;
  margin-left: 8px;
}

.tab-content {
  padding: 20px 0;
}

.empty-tip {
  text-align: center;
  color: #999;
  padding: 40px 0;
}

.dialog-content {
  padding: 20px 0;
}

.form-item {
  display: flex;
  align-items: center;
  padding: 16px 0;
  border-bottom: 1rpx solid #eee;
}

.form-label {
  width: 80px;
  flex-shrink: 0;
  color: rgba(0, 0, 0, 0.9);
  font-size: 14px;
  line-height: 22px;
  padding-right: 12px;
}

.t-radio-group {
  flex: 1;
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.color-picker {
  display: flex;
  align-items: center;
  margin-top: 20px;
}

.color-picker-label {
  width: 80px;
  flex-shrink: 0;
  color: rgba(0, 0, 0, 0.9);
  font-size: 14px;
  line-height: 22px;
  padding-right: 12px;
}

.color-options {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.color-box {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  border: 2px solid transparent;
  cursor: pointer;
  box-sizing: border-box;
}

.color-box.selected {
  border-color: #0052D9; /* 选中颜色边框 */
}

.event-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}